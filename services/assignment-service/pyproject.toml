[tool.ruff]
line-length = 100

[tool.ruff.lint]
# BLE001: blind Exception catch — intentional in Kafka consumer loops
# S112:   try-except-continue — intentional resilience pattern
ignore = ["BLE001", "S112"]
